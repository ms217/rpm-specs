#Before you can run the mysqld_exporter you'll have to setup a MySQL User with the required permissions

#Example:
#CREATE USER 'mysqld_exporter'@'localhost' IDENTIFIED BY 'XXXXXXXX';
#GRANT PROCESS, REPLICATION CLIENT, SELECT ON *.* TO 'mysqld_exporter'@'localhost' WITH MAX_USER_CONNECTIONS 3;
#GRANT SELECT ON performance_schema.* TO 'mysqld_exporter'@'localhost' WITH MAX_USER_CONNECTIONS 3;

[client]
user=mysqld_exporter
password=XXXXXXXX

